# Repair AI Frontend

Scaffolded premium mobile-first dashboard UI for Repair AI.

Scripts:

```
npm install
npm run dev
npm run build
npm run preview
```

Vercel: Build output is generated into `.vercel/output` by the Nitro/Vite build (compatible with Vercel Build Output API).
